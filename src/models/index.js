const joinModel = require('../models/joinModel');
const loginModel = require('../models/loginModel');
const favoritesCharactersModel = require('../models/favoritesCharactersModel');
const favoritesComicsModel = require('../models/favoritesComicsModel');

module.exports = {
  joinModel,
  loginModel,
  favoritesCharactersModel,
  favoritesComicsModel,
};